# Test Spec: Block Rendering Refactor

## Acceptance
1. Given pages with blocks, preview renders meaningful content.
2. Given pages with blocks, export path produces pptx without runtime error.
3. Given pages without blocks, legacy page.type rendering still works.
4. Frontend build passes.

## Verification Commands
- npm run build (frontend)
