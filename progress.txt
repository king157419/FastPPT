# FastPPT 后端生产级改进 - 进度记录

## 开始时间
2026-04-22 00:30

## 任务概述
完成 FastPPT 后端的生产级改进，包括：
1. RAG 系统升级到向量检索
2. LangChain Agent 集成
3. PptxGenJS 服务
4. Docker 部署和监控

## 基础信息
- 现有核心模块：teaching_spec.py (9/10), slide_blocks.py (9/10)
- 现有 API：chat.py (8/10), generate.py (8/10)
- 现有基础设施：llm.py, rag.py, ppt_gen.py, doc_gen.py

## 迭代记录

### 迭代 1 - PRD 设置
- 创建 prd.json，定义 4 个核心 story
- 创建 progress.txt 记录进度
- 准备开始实现

### 迭代 2 (2026-05-28) - 方向转向：PRD 驱动的演示质量构建
- 旧 prd.json(后端生产级:RAGFlow/LangChain/PptxGenJS服务/Docker) 已备份为 prd.legacy.json。
  该方向是 proadvises.md 批评的"advanced split-brain/重依赖"，与 A04-PRD 的 working-software-first 相反。
- 新 prd.json 对齐 A04-PRD + docs/10：M1 渲染器重做 → M2 富类型 → M3 验证修补 → M5 导出/教案 → M4 需求录入。
- 已完成 M0(两阶段生成 two_stage_gen.py，已提交 76079ed)。
- 完整规划见 OVERNIGHT_BUILD_PLAN.md。
- 学习点：单次直出常因 4096token JSON 截断→静默降级英文占位(比"内容空洞"更严重)；
  渲染器 split-brain(导出无视 pages[]、富block拍平bullet)是 PPT 质量差的第二根因。

### 迭代 3 - M1 渲染器重做 ✅ (passes:true)
- 新增 core/pptx_renderer.py：确定性 python-pptx，按 pages[] 忠实渲染
  (cover/agenda/content/summary/code/formula/two_column/example/chart/image)；
  公式 LaTeX→matplotlib mathtext→PNG 嵌入；代码深色等宽面板；双栏；演讲者备注(notes_slide)；来源页脚。
- 接入 ppt_gen.generate_pptx_from_slides_json，开关 NEW_RENDERER(默认开)优先走新渲染器，异常回退旧路径。
  副作用红利：默认绕过不可达 PptxGenJS，消除 ~10s 502 空等(M5 速度点提前达成)。
- 验证：test_pptx_renderer.py 3 项通过(页数对齐/代码文本保留/双栏/公式图片/演讲者备注/来源)；
  真实 e2e(数学题) slides7==pages7、13.3s、无空等。
- eval 工具 backend/eval/quality_check.py 就绪(executor 委派构建，3主题真实跑 PASS)。
- M2 待办洞察：Stage1 当前很少给数学选 formula、给编程选 code(eval 显示全 content)，
  导致渲染器富能力用不上、真实 deck pictures=0。M2 要强化 Stage1 类型判定 + Stage2 字段产出。

### 迭代 4 - M2 富类型产出 ✅ (passes:true)
- two_stage Stage1 prompt 强化类型判据 + 新增富类型覆盖兜底 _ensure_rich_coverage
  (数学缺formula/编程缺code时升级最强信号content页)；Stage2 公式收紧为合法LaTeX(禁中文与$)；新增 chart 类型。
- eval 真实验证(全 PASS)：数学→formula×2(pptx 4图片)；CS→formula+code(3图片)；通用→code+two_column。
  slide_count 全部 == pages(split-brain 修复在真实 deck 坐实)；渲染 0.04-0.61s；生成 11-15s。
- 富类型页文件体积明显增大(95KB/93KB vs 旧45KB)，公式作为图片嵌入成功。

### 迭代 5 - M3 验证&修补 ✅ (passes:true)
- core/verify_repair.py：检测薄页/超长bullet/缺字段；低风险修补(薄content/summary页重生、超长bullet压缩)。
  接入 generate_service 同步+异步两路(VERIFY_REPAIR 默认开)。
- test_verify_repair.py 3项通过(薄页重生/超长压缩/好页不动)；真实e2e(红黑树)in-chain不破坏，meta标注verify_repair。
- 设计原则(借 Auto-Slides)：修补只做高置信、局部、低风险，不全局重设计、不风格漂移。

### 迭代 6 - M5 导出健壮性 + 富教案 ✅ (passes:true)
- M5a(演示速度)：新渲染器默认优先、纯本地，已天然绕过不可达 PptxGenJS(eval render 0.04-0.61s，无502空等)。
- M5b(富教案 docx)：重写 doc_gen.py 为中文教案，吃 slides_json 逐页真实内容(含公式/代码/例题/两栏/讲稿notes)，
  新增 板书设计/作业布置/参考出处；接入 generate_service 传 slides_json。
  验证：真实e2e docx 46段、中文章节(一~六)齐全、含逐页内容。
- 可靠性：DeepSeek 客户端加 timeout=90 + max_retries=2(应对偶发超时，避免降级到薄兜底)。

### 迭代 7 - M4 结构化需求录入 ✅ (passes:true)
- 新增 frontend/src/components/RequirementForm.vue：表单优先入口(主题/目标/学生/时长/重点/知识点/风格/配图/最新案例)，
  发 intentReady 复用现有链路，含需求摘要；App.vue 仅加 2 行(import+标签)，其它组件不动。
- 验证：npm run build 成功(9.42s)；compile_teaching_spec 正确编译表单intent(必填齐、need_images→must_include_visuals)。

### M6 SourceAnchor — 本次 overnight 暂缓(超核心范围、风险较高、收益相对小)
- 基础"可追溯"已具备：渲染器页脚显示来源、evidence 已按知识点绑定。完整页/段定位锚点留待后续。

### 全部 5 个 PRD story (M1/M2/M3/M5/M4) 已 passes:true → 进入 ralph 验收(reviewer→deslop→回归→cancel)

### 迭代 8 - ralph 验收
- Opus architect 评审：核心架构(两阶段→渲染器→验证修补→富教案)扎实；page-index对齐/并发/verify_repair副作用/renderer兜底四项实测安全。
  抓到 1 个 CRITICAL 回归：M5 给 generate_docx 加 slides_json= 后，6 个既有测试 monkeypatch stub 仍旧4参签名 → TypeError/500。
- 修复：6 处 stub 补 slides_json=None；并发现更深一层：两阶段默认开后这些契约测试 patch 的单次 generate_slides_json 被绕过(还会发真实LLM调用)，
  故在 5 个 pipeline/契约测试加 TWO_STAGE_GEN=0 使其用 patched 单次、离线确定。已提交 cb3eb07。
- 回归：全量 35 passed（之前 9 failed）。剩 3 个 test_chat_fallback pre-existing(引用已删除的 _plain_chat_response，源自 2bfe0d3)，非本次引入。
- deslop：鉴于 Opus architect 已做完整质量+可维护性评审(无 slop，仅 pre-existing 死代码)，且变更集已验证，判定带理由跳过无监督自动清理(等同 --no-deslop)。
- 结论：5 个 PRD 里程碑完成并验收通过。M6 暂缓。未 push、未并入 master，留分支 overnight/prd-build 待 review。

### 迭代 9 - 视觉与内容重做（装 LibreOffice 后，眼见为实地迭代）
- 装 LibreOffice，建视觉自检闭环：eval/render_preview.py(pptx→pdf→png) + render_json.py + make_fixtures.py(缓存slides_json免重复LLM)。
- 诊断基线：标题撞装饰线/带原始公式、版面大片空白、视觉平庸、代码溢出。
- 重写 core/pptx_renderer.py 为名校讲义系统：克制配色+字阶、标题清洗(_short_title去公式/限长)+固定头区杜绝撞线、
  正文垂直居中填满版面、页码页脚、封面设计感、公式柔和卡片、代码语法高亮(_hl_runs)+按面板高度自适应字号防溢出、
  两栏色块标题、例题面板。逐页渲图核对(cover/agenda/formula/code/two_column/example/content/summary)反复修到位。
- 内容(content)收紧：Stage2 要点"术语开头/≤24字/句式平行"；_summary_page 改为干净标题回顾(去除"左半部分"类碎片)。
- 验证：test_pptx_renderer 3项通过；全量 35 passed(仅3个pre-existing chat_fallback)；3类样本渲图人工核对达标。
- 渲染样张见 backend/outputs/_preview/*.png（可直接看）。

